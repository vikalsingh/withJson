{
  "name": "wirh-json",
  "version": "1.0.0",
  "description": "In this we are playing with json file, read, delete, send data",
  "main": "app",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Vikal",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1"
  }
}
